# ورشة متنقلة لصيانة السيارات الألمانية والصينية بالرياض

هذا المشروع مبني باستخدام تقنيات حديثة: **Next.js 15**, **Tailwind CSS**, و **Genkit** للذكاء الاصطناعي.

## 🚀 خطوات رفع المشروع على GitHub (نفذها في جهازك)

بما أنني قمت بتجهيز كافة الملفات لك، اتبع هذه الخطوات البسيطة في سطر الأوامر (Terminal) داخل مجلد المشروع:

1. **تهيئة Git:** (إذا لم تفعّله من قبل)
   ```bash
   git init
   ```

2. **إضافة الملفات:**
   ```bash
   git add .
   ```

3. **تسجيل النسخة الأولى:**
   ```bash
   git commit -m "إطلاق النسخة الأولى للموقع بنجاح"
   ```

4. **تسمية الفرع الرئيسي:**
   ```bash
   git branch -M main
   ```

5. **الربط بمستودعك على GitHub:**
   - أنشئ مستودعاً جديداً (New Repository) في حسابك على GitHub.
   - انسخ الرابط الذي سينتهي بـ `.git`.
   - **إذا كنت تربط المستودع لأول مرة:**
     ```bash
     git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
     ```
   - **إذا كنت تريد تغيير رابط مستودع قديم:**
     ```bash
     git remote set-url origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
     ```

6. **الرفع النهائي:**
   ```bash
   git push -u origin main
   ```

---

### 🚨 ملاحظة هامة جداً بخصوص المصادقة (إذا فشل الرفع)

إذا واجهت خطأ مثل `Password authentication is not supported` أو `Permission denied`، فهذا لأن GitHub لم يعد يقبل كلمة المرور العادية لرفع الملفات عبر سطر الأوامر.

**الحل هو استخدام Personal Access Token (رمز وصول شخصي):**

1.  اذهب إلى حسابك في GitHub ثم: **Settings > Developer settings > Personal access tokens > Tokens (classic)**.
2.  اضغط **Generate new token (classic)**.
3.  أعطِ الرمز اسماً (مثلاً `my-app-token`) وحدد صلاحية (`Expiration`).
4.  **الأهم:** ضع علامة صح (✅) بجانب `repo`.
5.  اضغط **Generate token** وانسخ الرمز الذي سيظهر لك.
6.  نفذ أمر `git push` مرة أخرى. **وعندما يطلب منك كلمة المرور (Password)، الصق الرمز الذي نسخته بدلاً من كلمة مرور حسابك.**

---

## 💡 ملاحظات هامة للنشر
- عند نشر الموقع على (Vercel أو Firebase)، تأكد من إضافة مفتاح الـ API الخاص بك في الإعدادات (Environment Variables) تحت اسم:
  `GOOGLE_GENAI_API_KEY`
